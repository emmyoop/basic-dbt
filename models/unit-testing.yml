unit_tests:
  - name: test_mixed_csv
    model: clean_table
    description: "file csv test"
    given:
      - input: ref('even_id_table')
        format: csv
        fixture: even_id_fixture
    expect:
      format: csv
      fixture: output_fixture
